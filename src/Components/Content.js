import React, { Fragment } from "react";
import Projects from "./project/Projects";

const Content = () => {
  return (
    <Fragment>
      <Projects />
    </Fragment>
  );
};

export default Content;
